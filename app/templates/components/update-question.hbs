{{#if updateQuestionForm}}
<h4>Update {{question.author}}'s {{question.question}}</h4>
<div>
  <form>
    <div class="form-group">
      <label for="author">Author</label>
      {{input value=question.author id="author"}}
    </div>
    <div class="form-group">
      <label for="question">Question</label>
      {{input value=question.question id="question"}}
    </div>
    <div class="form-group">
      <label for="notes">Notes</label>
      {{input value=question.notes id="notes"}}
    </div>
  </form>
  <button {{action 'save1'}}>Save</button>
</div>
{{else}}
<button {{action 'questionFormShow'}}>Update Question</button>

{{/if}}
